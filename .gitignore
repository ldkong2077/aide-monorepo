node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.env.*.local
*.log
logs/
.aide/
data/
coverage/
docs/api/
docs/_site/
.DS_Store
Thumbs.db
# AIDE installer artefacts — auto-generated by `aide init` per project.
# Project-local MCP config and agent rules files should not be committed
# to source control (the global equivalents at ~/.config/<agent>/ do the
# job for the user across all their projects).
.mcp.json
.claude/
.cursor/
.opencode/
AGENTS.md
CLAUDE.md
# Per-package build artefacts
packages/*/dist/
packages/*/tsconfig.tsbuildinfo
# Stale / no-longer-used scratch directory
_staging/
# Per-package node_modules from a buggy npm install
packages/*/node_modules/
# Reference directories
superpowers-main/
# Temporary files
ai工具评估.txt
err.log
err2.log
